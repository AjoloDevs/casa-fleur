generator client {
  provider = "prisma-client-js"
  output   = "../src/generated" 
}

datasource db {
  provider = "postgresql"
}

model User {
  idUser    String    @id     @default(uuid()) 
  name      String   
  email     String    @unique
  password  String    
  numberPhone     String 
  city      String
}
